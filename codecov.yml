coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0%
    patch:
      default:
        target: 100%
        threshold: 0%
comment:
  layout: "diff, flags, files"
  require_changes: false
  hide_project_coverage: false
